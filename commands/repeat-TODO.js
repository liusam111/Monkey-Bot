const MIN_REPEATS = 3;

module.exports = {
    name: 'repeat',
    description: 'Easter egg. Repeats the last message sent if the past 3 messages are the same',
    cannotRun: true,
    async execute(params){
        //TODO: REVAMP THIS
        return;
    }
}